<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5">

    <LinearLayout
        android:id="@+id/llHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Banner with Logo and Slogan -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:background="#4A90E2"
            android:padding="12dp">

            <!-- Logo Lilly Milk (Small version) -->
            <ImageView
                android:id="@+id/imgLogo"
                android:layout_width="50dp"
                android:layout_height="50dp"
                android:layout_alignParentStart="true"
                android:layout_alignParentTop="true"
                android:background="@android:color/white"
                android:scaleType="fitCenter"
                android:padding="4dp"
                android:src="@drawable/logo" />

            <!-- Slogan -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignBottom="@id/imgLogo"
                android:layout_marginStart="12dp"
                android:layout_toEndOf="@id/imgLogo"
                android:layout_marginBottom="4dp"
                android:text="Tinh túy từ thiên nhiên - nuôi dưỡng mỗi ngày"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="italic" />

            <!-- Hình chiếc lá trang trí -->
            <ImageView
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_below="@id/imgLogo"
                android:layout_alignParentStart="true"
                android:layout_marginTop="4dp"
                android:src="@android:drawable/ic_menu_gallery"
                android:tint="#8BC34A" />

        </RelativeLayout>

        <!-- Search Bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="10dp">

            <EditText
                android:id="@+id/etSearchMain"
                android:layout_width="0dp"
                android:layout_height="40dp"
                android:layout_weight="1"
                android:background="@drawable/bg_search_box"
                android:hint="Bạn muốn tìm sản phẩm gì?"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:textSize="12sp"
                android:focusable="false"
                android:clickable="true" />

            <!-- Hình giỏ hàng -->
            <ImageView
                android:id="@+id/imgCartMain"
                android:layout_width="35dp"
                android:layout_height="35dp"
                android:layout_marginStart="10dp"
                android:src="@drawable/ic_cart"
                android:scaleType="fitCenter" />
        </LinearLayout>

        <!-- Success Message Bar -->
        <TextView
            android:id="@+id/tvSuccessMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#78D18C"
            android:gravity="center"
            android:padding="8dp"
            android:text="Thêm vào giỏ thành công"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:alpha="0.8"
            android:visibility="gone" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProduct"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/bottomNav"
        android:layout_below="@+id/llHeader"
        android:padding="4dp" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@android:color/white"
        app:itemIconTint="#4A90E2"
        app:itemTextColor="#4A90E2"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/menu_bottom" />

</RelativeLayout>